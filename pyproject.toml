[project]
name = "discord-drive-bot"
version = "0.1.0"
description = "Discord bot for organizing project documents in Google Drive"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
